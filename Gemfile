source 'https://rubygems.org'

gem 'roda'

gem 'sequel'
gem 'sequel_pg'

gem 'tilt'
gem 'erubi'
# gem 'sass'  # sass is being handled by node-sass

group :development, :test do
  gem 'pry'
end

group :test do
  gem 'capybara'
end

